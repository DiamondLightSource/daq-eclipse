language: java

# whitelist
branches:
 only:
  - master
 
jdk:
 - oraclejdk8

before_install:
 - chmod +x ./org.eclipse.scanning.releng/build/commit-msg.sh
 - bash ./org.eclipse.scanning.releng/build/commit-msg.sh
 
# Maven compile and javadoc TODO run the tests in org.eclipse.scanning.test, might not be able to run all of them
script: mvn compile javadoc:javadoc
