/**
 * @Deprecated Package contains annotations for the metawidgets library which is
 * deprecated as a dependency because it contains copied code from google and relicenses
 * it illegally. The preferred solution is to use @FieldDescriptor which does the same 
 * thing in one large annotation that these do in separate ones. It renders in a locally
 * coded UI which has no third party liability other than eclipse.
 */
package org.eclipse.scanning.api.annotation;
