/**
 * 
 * The package org.eclipse.scanning.api reflects interfaces such as scannable <br>
 * and detector from the GDA8 architecture. So that GDA8 devices are beckwardsly<br>
 * compatible, it can decided in the first version to keep method names the same.<br><br>
 * The mapping scanning and XPDF scanning which are the first targets for GDA9<br>
 * will be implemented with devices compatible with GDA8 so that new devices do <br>
 * not have to be created.<br>
 */
package org.eclipse.scanning.api;